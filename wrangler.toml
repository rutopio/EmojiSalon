name = "emojisalon"
pages_build_output_dir = "dist"
compatibility_date = "2026-06-21"
# resvg-wasm pulls in node:stream when bundled into the Functions worker.
compatibility_flags = ["nodejs_compat"]
