node_modules

# Ignore lockfiles we don't use
# package-lock.json
# yarn.lock
# pnpm-lock.yaml
# bun.lock

.DS_Store
.cache
.env

.data
.vercel
.output
.wrangler
.netlify
dist
/build/
/api/
/server/build
/public/build

.tanstack
.vscode

# Cloned upstream emoji source for preprocessing (re-fetched by `pnpm preprocess`)
preprocess/twemoji

# Downloaded COLR font files used for CPAL map extraction
preprocess/fonts
/tasks
